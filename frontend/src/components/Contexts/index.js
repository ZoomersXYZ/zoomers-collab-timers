import { createContext} from "react";
const GroupContext = createContext( null );
const RoomContext = createContext( null );
const SocketContext = createContext( null );

export { GroupContext, RoomContext, SocketContext };
